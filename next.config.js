/** @type {import('next').NextConfig} */
const nextConfig = {
  // No output: 'export' for dynamic deployment
  
  // Regular Next.js configuration
  experimental: {
    appDir: true,
  },
}

module.exports = nextConfig
